name: Build Cross-Platform Executables

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Debug - List files
        run: |
          dir
          type requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller pillow
          pip install -r requirements.txt
          pip list

      - name: Build Windows executable
        run: |
          python -m PyInstaller --version
          python build.py --onefile --archive --verbose
        timeout-minutes: 45

      - name: Debug - Check dist folder
        if: always()
        run: |
          if exist dist (
            dir dist /s
          ) else (
            echo "dist folder not found"
          )

      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: NetworkAIMonitor-Windows
          path: dist/*
          if-no-files-found: warn

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller pillow
          pip install -r requirements.txt
          pip list

      - name: Build macOS executable
        run: |
          python -m PyInstaller --version
          python build.py --onefile --archive --verbose
        timeout-minutes: 45

      - name: Debug - Check dist folder
        if: always()
        run: |
          ls -la dist/ 2>/dev/null || echo "dist folder not found"

      - name: Upload macOS executable
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: NetworkAIMonitor-macOS
          path: dist/*
          if-no-files-found: warn

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-glx libxkbcommon-x11-0 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libegl1

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller pillow
          pip install -r requirements.txt
          pip list

      - name: Build Linux executable
        run: |
          python -m PyInstaller --version
          python build.py --onefile --archive --verbose
        timeout-minutes: 45

      - name: Debug - Check dist folder
        if: always()
        run: |
          ls -la dist/ 2>/dev/null || echo "dist folder not found"

      - name: Upload Linux executable
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: NetworkAIMonitor-Linux
          path: dist/*
          if-no-files-found: warn

  release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: |
          ls -la artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/*
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
